# Phase 1 R validation/import utilities

These scripts are researcher-side QA tools only. They are not production collection dependencies.

Suggested packages: readtext, jsonlite, arrow, httr2, dplyr, tidyr, renv.

Use synthetic/exported public-safe data only in examples. Activate an independent renv project here
before operational use; do not couple R package installation to Python or browser startup.
